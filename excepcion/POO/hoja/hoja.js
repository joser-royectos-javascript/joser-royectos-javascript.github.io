
"use strict";
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class Hoja {
    constructor(imagen, x, y, v, t) {
        this.x = x;     // posición eje x
        this.y = y;     // posición eje y
        this.v = v;     // v = velocidad
        this.t = t;     // t = tamaño
        this.detenerBucle = false;

        try {
            this.iniciarBucle();
        } catch (error) {
            console.error("Error:", error.message);
        }
    }

    iniciarBucle() {
        const intervalId = setInterval(() => {
            if (this.detenerBucle) {
                clearInterval(intervalId);
                console.log("Bucle detenido.");
            } else {
                this.mover();
            }
        }, 17);
    }

    mover() {
        console.log("Moviendo ...");
        this.y = this.y + this.v;

        // Simulando alguna condición que podría detener el bucle
        if (this.y >= 100) {
            this.detenerBucle = true;
        }

        console.log(this.y);
    }
    pintar(){
        
    }
}

let hoja1 = new Hoja("hoja.js", 100, 0, 1, 100);
console.log("Fin");



